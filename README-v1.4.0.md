# INKAMNET Control Center v1.4.0

Upgrade operasional: Customer ID helper per-site, email auto/manual, Cluster/ODP XLSX import-export, ticket detail + daily progress + KPI teknisi, weekly duty rotation, invoice PDF, Report Center modern PDF, dan dashboard tanpa inventory KPI.

## Database
`ticket_updates` dibuat otomatis (CREATE TABLE IF NOT EXISTS) saat aplikasi startup, sehingga aman untuk existing database.
